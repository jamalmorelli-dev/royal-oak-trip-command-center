{
  "name": "royal-oak-trip-command-center",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next build && touch out/.nojekyll",
    "deploy": "rm -rf out && next build && touch out/.nojekyll && cd out && git init -b gh-pages && git add -A && git commit -m 'Deploy to GitHub Pages' && git push -f https://github.com/jamalmorelli-dev/royal-oak-trip-command-center.git gh-pages",
    "check-flight": "node bin/check-flight.js",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.5.25",
    "openai": "5.19.1",
    "react": "19.1.1",
    "react-dom": "19.1.1"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
